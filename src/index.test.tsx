import { Grid, Surface } from '.'

describe('Grid', () => {
  it('is truthy', () => {
    expect(Grid).toBeTruthy()
  })
})

describe('Surface', () => {
  it('is truthy', () => {
    expect(Surface).toBeTruthy()
  })
})
